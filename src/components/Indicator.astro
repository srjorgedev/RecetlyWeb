<div class="indicator">
    <svg
        width="auto"
        height="auto"
        viewBox="0 0 48 48"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
    >
        <rect width="auto" height="auto" fill="white" fill-opacity="0.01"></rect>
        <path
            d="M37 18L25 30L13 18"
            stroke-width="4"
            stroke-linecap="round"
            stroke-linejoin="round"></path>
    </svg>
</div>

<style>
    div.indicator {
        width: 50px;
        aspect-ratio: 1;
        position: absolute;
        left: 50%;
        bottom: 2.5rem;
        transform: translateX(-50%);
        animation: float infinite 2s;
        display: flex;
        align-items: center;
        justify-content: center;

        & svg {
            stroke: var(--color);
        }
    }

    @keyframes float {
        0% {
            transform: translate(-50%, 0);
        }
        50% {
            transform: translate(-50%, 16px);
        }
        100% {
            transform: translate(-50%, 0);
        }
    }
</style>
